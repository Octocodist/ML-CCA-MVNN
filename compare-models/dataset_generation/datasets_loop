#
#domains=("lsvm" "gsvm" "srvm" "mrvm")

domains=("gsvm")


for seed in {265..266}
do 
   echo "Start Seed " $seed 
   for dom in "${domains[@]}"
   do 
       python dataset.py -s --mode $dom  --seed $seed 
   done 
   echo "Seed done"
done






